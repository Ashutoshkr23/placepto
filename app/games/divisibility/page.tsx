import { DivisibilityGame } from "@/components/games/divisibility-game";

export const metadata = { title: "Divisibility Dash — Placepto" };

export default function DivisibilityPage() { return <DivisibilityGame />; }
